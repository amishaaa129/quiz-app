<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Score</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container results">
    <h1>Your Score: <%= correctAnswers %>/<%= totalQuestions %></h1>
    <h3>Your Highest Score: <%= max_score %></h3>
    <p><strong>Remark:</strong> <%= remark %></p>
</div>

    <script>
        // Replace the current page with the results page in the browser's history
        history.replaceState(null, null, location.href);

        // Block back navigation and keep users on the results page
        window.onpopstate = function () {
            history.replaceState(null, null, location.href);
            alert("You cannot go back after completing the quiz!");
        };
    </script>
</body>
</html>